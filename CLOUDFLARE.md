# Cloudflare Pages settings

- Production branch: `main`
- Root directory: repository root
- Build command: `npm run build`
- Build output directory: `.`
- Environment variable (Production + Preview): `BO_API_BASE=https://YOUR-BACKEND-DOMAIN/api`

For a Git-integrated Pages project, keep automatic production deployment enabled on `main`. Preview deployments may be enabled for feature branches.
