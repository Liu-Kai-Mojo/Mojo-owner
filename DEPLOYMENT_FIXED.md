# Owner frontend — repaired deployment

1. Install: `npm install`
2. Build: `npm run build`
3. Cloudflare Pages output directory: `dist`
4. The build now produces a complete standalone owner site at `dist/`.
5. API defaults to `https://boxoffice-platform-production.up.railway.app`.
6. Production routes: `/login`, `/`, `/admins`, `/clients`, `/settings`.
